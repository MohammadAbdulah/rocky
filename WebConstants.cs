using System;
using System.IO;

namespace Rocky
{
    public static class WebConstants
    {
        private static char separator = Path.DirectorySeparatorChar;
        public static string ImagePath = $"{separator}images{separator}product{separator}";
    }
}
